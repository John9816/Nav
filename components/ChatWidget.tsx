import React from 'react';

// This component has been deprecated and replaced by AIStudio.
// Keeping an empty render to prevent import errors if lazily loaded elsewhere, 
// though it is removed from App.tsx.
const ChatWidget: React.FC = () => {
  return null;
};

export default ChatWidget;